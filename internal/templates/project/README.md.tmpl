# {{.ProjectName}}

A microservice built with Go and gRPC.

## Features

- gRPC for communication
{{if .WithDB}}- Database integration with {{.DBType}}{{end}}
{{if .WithDocker}}- Docker containerization{{end}}
{{if .WithMiddleware}}- Middleware support (logging, auth, recovery){{end}}
{{if .WithTests}}- Comprehensive testing{{end}}
- Configuration management with Viper
- Graceful shutdown

## Getting Started

### Prerequisites

- Go 1.21+
- Protocol Buffers compiler (protoc)
{{if .WithDB}}- {{.DBType}} database{{end}}

### Installation

1. Clone the repository
2. Install dependencies: `go mod download`
3. Generate protobuf code: `make proto`
4. Build the application: `make build`

### Running the Application

```bash
# Run locally
make run

# Or using Docker
make docker-compose-up